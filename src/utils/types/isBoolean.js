/**
 * 判断是否为 Boolean 类型值
 * =============================================================
 * @param {String} val - 待检测的字符串
 * @returns {boolean}
 */
const isBoolean = (val) => {
  return typeof val === 'boolean'
}

export default isBoolean
