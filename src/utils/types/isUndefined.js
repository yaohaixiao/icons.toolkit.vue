/**
 * 判断是否 Undefined
 * =============================================================
 * @param {any} val
 * @returns {boolean}
 */
const isUndefined = (val) => {
  return val === 'undefined'
}

export default isUndefined
